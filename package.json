{
  "name": "plug-login",
  "description": "Logs in to plug.dj.",
  "version": "1.2.0",
  "license": "MIT",
  "keywords": [
    "plug",
    "plug.dj",
    "login"
  ],
  "main": "lib/index",
  "repository": "goto-bus-stop/plug-login",
  "bugs": "https://github.com/goto-bus-stop/plug-login/issues",
  "author": "Renée Kooi <renee@kooi.me>",
  "engines": {
    "node": ">= 4"
  },
  "dependencies": {
    "cookie": "^0.3.1",
    "node-fetch": "^2.1.2",
    "promise-props": "^1.0.0"
  },
  "devDependencies": {
    "buble": "^0.19.0",
    "mocha": "^5.0.0",
    "standard": "^11.0.0"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "buble --output lib --target node:4 --objectAssign Object.assign src",
    "test": "npm run build && mocha && standard"
  }
}
